"use client";

import React from "react";
import AddRoleMain from "@/components/admin/admin-control/AddRoleMain";

export default function EditAdminPage() {
  // AddRoleMain already contains the logic to detect the ID via useParams()
  return <AddRoleMain />;
}